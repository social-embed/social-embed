---
id: installation
title: Installation
description: How to setup the @social-embed web component.
slug: wc/installation
sidebar:
  order: 2
---

import { Aside } from '@astrojs/starlight/components';
import { Tabs, TabItem } from '@astrojs/starlight/components';

<Aside type="tip">
Just want the lib functions and patterns? See [@social-embed/lib](/lib/).
</Aside>

## Install

`<o-embed>` is distributed on npm, so you can install it locally or use it via npm CDNs like unpkg.com.

### Local Installation

<Tabs syncKey="pkg">
<TabItem label="npm">

```bash
npm i @social-embed/wc
```

</TabItem>

<TabItem label="yarn">

```bash
yarn add @social-embed/wc
```

</TabItem>

<TabItem label="pnpm">

```bash
pnpm add @social-embed/wc
```

</TabItem>
</Tabs>

### CDN

npm CDNs like [unpkg.com](https://unpkg.com) can directly serve files that have been published to npm. This works great for standard JavaScript modules that the browser can load natively.

For this element to work from unpkg.com specifically, you need to include the `?module` query parameter, which tells unpkg.com to rewrite "bare" module specificers to full URLs.

#### HTML

```html
<script type="module" src="https://unpkg.com/@social-embed/wc?module"></script>

<o-embed url="https://youtu.be/Bd8_vO5zrjo" allowfullscreen></o-embed>
<o-embed
  url="https://www.youtube.com/watch?v=NB5hH3ksvKE"
  allowfullscreen
></o-embed>
```

Try on [codepen](https://codepen.io/attachment/pen/poRRwdy), check "Playground" in the top menu for more!

### JavaScript

```js
import { OEmbedElement } from "https://unpkg.com/@social-embed/wc?module";
```
