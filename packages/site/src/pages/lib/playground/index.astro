---
import { LibPlayground } from "../../../components/lib-playground/LibPlayground";
/**
 * Library Playground - Interactive playground for testing @social-embed/lib.
 * Uses BaseLayout instead of StarlightPage.
 */
import BaseLayout from "../../../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Library Playground"
  description="Interactive playground for testing @social-embed/lib functions"
>
  <main class="playground-container">
    <LibPlayground client:only="react" mode="full" className="playground-content" />
  </main>
</BaseLayout>

<style>
  .playground-container {
    max-width: 800px;
    margin: 0 auto;
    padding: 2rem 1rem;
    flex: 1;
  }

  .playground-container :global(.playground-content) {
    background: var(--color-slate-100);
    border-radius: 0.5rem;
    padding: 1.5rem;
    border: 1px solid var(--color-slate-200);
  }

  [data-theme="dark"] .playground-container :global(.playground-content) {
    background: var(--color-slate-800);
    border-color: var(--color-slate-700);
  }
</style>
