/**
 * Global constants injected by Vite at build time.
 */

/** Current git branch name, injected via astro.config.ts define */
declare const __GIT_BRANCH__: string;

/** Current git commit SHA, injected via astro.config.ts define */
declare const __GIT_SHA__: string;
